export interface ProjectType {
  name: string;
  description: string;
}

export interface ProjectResType {
  status: string;
  name: string;
  describe: string;
}

export interface UsersResType {
  status: string;
  names: string[];
}
